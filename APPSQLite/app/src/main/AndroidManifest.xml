<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appsqlite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.APPSQLite">
        <activity
            android:name=".MainActivityEliminar"
            android:exported="true" />
        <activity
            android:name=".MainActivityActualizar"
            android:exported="true" />
        <activity
            android:name=".MainActivityDetalle"
            android:exported="true" />
        <activity android:name=".MainActivityMostrar" />
        <activity android:name=".MainActivityBuscar" />
        <activity android:name=".MainActivityInsertar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>